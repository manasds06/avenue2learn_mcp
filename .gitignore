# ---- Credentials and local state (NEVER commit) ----
# The session file is equivalent to a logged-in Avenue session.
.avenue-mcp/
session.json
*.session.json
storage_state.json
# login.py writes via `Path.with_suffix(".tmp")`, which REPLACES the extension:
# session.json -> session.tmp. A crash or a failed rename can leave that file
# holding the whole cookie jar, so the temp name needs covering too.
*.tmp
# Per-institution logs (settings.log_dir). writes.log records submissions.
logs/

# Downloaded course material (instructor IP) and the local index
cache/
index.db
index.db-*
vectors.npy

# Probe output may contain PII / real IDs
tests/fixtures/
docs/probe-raw/
# `probe.py --out FILE` defaults to wherever you run it, i.e. the repo root. Its
# summary is redacted (field names and types, never values) but it still records
# real org_unit_ids and route inventories, so it does not belong in git. Write-ups
# go in docs/NN-*-probe-results.md by hand instead.
*probe*.json
probe-*.json

# ---- Python ----
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# ---- Editors / OS ----
.vscode/
.idea/
.DS_Store
Thumbs.db
